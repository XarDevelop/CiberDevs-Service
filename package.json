{
  "name": "ciberdevs",
  "private": true,
  "type": "module",
  "scripts": {
    "vercel-build": "cd backend && npm install && npm run build && cd ../frontend && npm install && npm run build"
  }
}
